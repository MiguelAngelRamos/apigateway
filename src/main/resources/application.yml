spring:
  application:
    name: gateway
  profiles:
    default: eureka-on-circuit-breaker
server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Corrige el typo en 'defaultZone'